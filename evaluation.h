#pragma once

#include "graph.h"

int evaluateFinishedWithTime(vector<Car> &cars, int duration);

int evaluateFinishedWithPercentage(vector<Car> &cars, int duration);

int getPercentageDone(Car* car);


